# Security policy

Report vulnerabilities privately through GitHub security advisories.

This library validates structure; it does not sanitize HTML, SQL, shell commands, URLs, medical advice, or authorization decisions. Applications must still apply context-specific validation and access control after parsing.
